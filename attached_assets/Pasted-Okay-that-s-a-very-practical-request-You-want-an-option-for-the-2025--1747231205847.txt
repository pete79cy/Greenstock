Okay, that's a very practical request\! You want an option for the "ΔΗΛΩΣΗ ΚΑΛΛΙΕΡΓΕΙΑΣ ΓΙΑ ΤΟ 2025" report to exclude plants that have a quantity of zero. This will help make the report cleaner and more focused on plants that are actually in stock.

Here's how we can implement this:

1.  **Frontend Update**: We'll add a checkbox on the page near the report generation button. You can tick this box if you want to exclude plants with zero quantity.
2.  **Backend Update**: The API that generates the PDF will be modified to check if this option is selected. If it is, it will filter out the zero-quantity plants *before* generating the PDF.

Let's get this done\!
Excellent, adding an option to exclude zero-quantity plants from the report is a smart move for cleaner, more relevant declarations\!

Here's how we'll implement this:

1.  **Frontend Update (`src/pages/Index.tsx`)**:

      * We'll add a checkbox labeled something like "Exclude plants with zero quantity" (or the Greek equivalent if you prefer for UI consistency) near the "Generate Cultivation Report (PDF)" button.
      * The state of this checkbox (whether it's checked or not) will be tracked.
      * When you click to generate the report, this preference will be sent to the backend.

2.  **Backend API Update (`src/pages/api/reports/cultivation-declaration.ts`)**:

      * The API will now look for this new option (e.g., a query parameter like `excludeZero=true`).
      * If the option to exclude zero-quantity plants is active, the database query will be modified to fetch only plants with a quantity greater than zero before generating the PDF.

This will give you direct control over the content of your report.

\<lov-code\>
\<lov-thinking\>
The user wants a checkbox on the frontend to control whether plants with zero quantity are included in the "ΔΗΛΩΣΗ ΚΑΛΛΙΕΡΓΕΙΑΣ ΓΙΑ ΤΟ 2025" PDF report.

**Frontend (`src/pages/Index.tsx`) changes:**

1.  Add a new state variable for the checkbox, e.g., `const [excludeZeroQuantity, setExcludeZeroQuantity] = useState(false);`.
2.  Import and add the `Checkbox` and `Label` components from `shadcn/ui` to the JSX. This will be placed near the report generation button.
    ```jsx
    <div className="flex items-center space-x-2 my-2">
      <Checkbox
        id="excludeZero"
        checked={excludeZeroQuantity}
        onCheckedChange={(checked) => setExcludeZeroQuantity(Boolean(checked))}
      />
      <Label htmlFor="excludeZero" className="text-sm font-medium text-[#333333]">
        Exclude plants with zero quantity
      </Label>
    </div>
    ```
3.  Modify `handleGenerateCultivationReport` to pass the `excludeZeroQuantity` state as a query parameter to the API.
    The URL will change from `/api/reports/cultivation-declaration` to `/api/reports/cultivation-declaration?excludeZero=${excludeZeroQuantity}`.

**Backend (`src/pages/api/reports/cultivation-declaration.ts`) changes:**

1.  Import `gt` (greater than) operator from `drizzle-orm`.
2.  In the `handler` function, read the `excludeZero` query parameter from `req.query`.
3.  Conditionally add a `where` clause to the Drizzle database query. If `excludeZero` is `'true'`, add `where(gt(plantsTable.quantity, 0))`.

This seems like a straightforward enhancement. I need to ensure the `Checkbox` component from `shadcn/ui` is available. Based on `current-code`, `checkbox.tsx` is one of the forbidden-to-edit UI components, which means it should be installed and usable. Similarly for `Label`.
\</lov-thinking\>

I'll update both the frontend (`src/pages/Index.tsx`) to include the new checkbox option and the backend API (`src/pages/api/reports/cultivation-declaration.ts`) to process this option.

**Step 1: Update Frontend (`src/pages/Index.tsx`)**
We'll add a checkbox and update the report generation function.

\<lov-write file\_path="src/pages/Index.tsx"\>
// ... (keep existing imports: useState, useEffect, useRef, useQuery, useQueryClient, Button, Table etc.)
import { useState, useEffect, useRef } from 'react';
import { useQuery, useQueryClient } from '@tanstack/react-query';
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { Checkbox } from "@/components/ui/checkbox"; // Import Checkbox
import { Label } from "@/components/ui/label"; // Import Label
import { toast } from "@/hooks/use-toast";
import useDebounce from '@/hooks/useDebounce';
import type { Plant } from './api/plants';
import {
AlertDialog,
AlertDialogAction,
AlertDialogCancel,
AlertDialogContent,
AlertDialogDescription,
AlertDialogFooter,
AlertDialogHeader,
AlertDialogTitle,
AlertDialogTrigger,
} from "@/components/ui/alert-dialog";
import { Download } from 'lucide-react';

const fetchPlants = async (searchTerm: string = ""): Promise\<Plant[]\> =\> {
// ... (keep existing fetchPlants function)
console.log(`Workspaceing plants with search term: "${searchTerm}"`);
const response = await fetch(`/api/plants?search=${encodeURIComponent(searchTerm)}`);
if (\!response.ok) {
const errorData = await response.json().catch(() =\> ({ message: "Unknown error" }));
console.error("Failed to fetch plants:", errorData);
throw new Error(errorData.message || 'Network response was not ok');
}
const data = await response.json();
console.log("Plants fetched successfully:", data);
return data;
};

const Index = () =\> {
// ... (keep existing state: queryClient, searchTerm, debouncedSearchTerm, isRestoring, fileInputRef)
const queryClient = useQueryClient();
const [searchTerm, setSearchTerm] = useState('');
const debouncedSearchTerm = useDebounce(searchTerm, 500);
const [isRestoring, setIsRestoring] = useState(false);
const fileInputRef = useRef\<HTMLInputElement\>(null);
const [excludeZeroQuantity, setExcludeZeroQuantity] = useState(false); // New state for the checkbox

const { data: plants, isLoading, error, refetch } = useQuery\<Plant[], Error\>({
queryKey: ['plants', debouncedSearchTerm],
queryFn: () =\> fetchPlants(debouncedSearchTerm),
});

useEffect(() =\> {
// console.log("Search term changed or component mounted, debounced term:", debouncedSearchTerm);
}, [debouncedSearchTerm]);

const handleSearchChange = (event: React.ChangeEvent\<HTMLInputElement\>) =\> {
setSearchTerm(event.target.value);
};

const handleBackupData = async () =\> {
// ... (keep existing handleBackupData function)
console.log("Attempting to backup data...");
try {
const response = await fetch('/api/backup/create');
if (\!response.ok) {
const errorData = await response.json().catch(() =\> ({ message: "Backup failed" }));
throw new Error(errorData.message || 'Backup request failed');
}
const blob = await response.blob();
const url = window.URL.createObjectURL(blob);
const a = document.createElement('a');
const contentDisposition = response.headers.get('Content-Disposition');
let filename = 'plant-inventory-backup.json';
if (contentDisposition) {
const filenameMatch = contentDisposition.match(/filename="?([^"]+)"?/);
if (filenameMatch && filenameMatch.length \> 1) {
filename = filenameMatch[1];
}
}
a.href = url;
a.download = filename;
document.body.appendChild(a);
a.click();
a.remove();
window.URL.revokeObjectURL(url);
toast({ title: "Backup Successful", description: `Data backed up to ${filename}` });
console.log("Backup successful, file downloaded.");
} catch (err: any) {
console.error("Backup failed:", err);
toast({ variant: "destructive", title: "Backup Failed", description: err.message });
}
};

const handleRestoreData = async (event: React.ChangeEvent\<HTMLInputElement\>) =\> {
// ... (keep existing handleRestoreData function)
const file = event.target.files?.[0];
if (\!file) {
toast({ variant: "destructive", title: "Restore Error", description: "No file selected." });
return;
}
setIsRestoring(true);
const formData = new FormData();
formData.append('backupFile', file);
console.log(`Attempting to restore data from ${file.name}...`);
try {
const response = await fetch('/api/backup/restore', {
method: 'POST',
body: formData,
});
const result = await response.json();
if (\!response.ok) {
throw new Error(result.message || 'Restore request failed');
}
toast({ title: "Restore Successful", description: `${result.restoredCount || 0} plants restored. Refreshing data.` });
console.log("Restore successful. Invalidating plants query to refetch.");
queryClient.invalidateQueries({ queryKey: ['plants'] });
} catch (err: any) {
console.error("Restore failed:", err);
toast({ variant: "destructive", title: "Restore Failed", description: err.message });
} finally {
setIsRestoring(false);
if (fileInputRef.current) {
fileInputRef.current.value = "";
}
}
};

const handleGenerateCultivationReport = async (event?: React.MouseEvent\<HTMLButtonElement\>) =\> {
if (event) {
event.preventDefault();
}
console.log(`handleGenerateCultivationReport called. Exclude zero quantity: ${excludeZeroQuantity}`);
toast({ title: "Generating Report", description: "Please wait..." });

```
try {
  // Pass excludeZeroQuantity state as a query parameter
  const response = await fetch(`/api/reports/cultivation-declaration?excludeZero=${excludeZeroQuantity}`);
  if (!response.ok) {
    let errorDetail = 'Report generation request failed';
    try {
        const errorData = await response.json();
        errorDetail = errorData.message || errorDetail;
    } catch (jsonError) {
        errorDetail = response.statusText || errorDetail;
        console.error("Response was not JSON:", await response.text());
    }
    console.error("Report generation API call failed:", errorDetail);
    throw new Error(errorDetail);
  }

  const blob = await response.blob();
  const url = window.URL.createObjectURL(blob);
  const a = document.createElement('a');
  
  const contentDisposition = response.headers.get('Content-Disposition');
  let filename = 'cultivation-report.pdf';
  if (contentDisposition) {
    const filenameMatch = contentDisposition.match(/filename="?([^"]+)"?/);
    if (filenameMatch && filenameMatch.length > 1) {
      filename = filenameMatch[1];
    }
  }
  
  a.href = url;
  a.download = filename;
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  window.URL.revokeObjectURL(url);
  
  toast({ title: "Report Generated", description: `Report downloaded as ${filename}` });
  console.log("Report PDF downloaded successfully.");

} catch (err: any) {
  console.error("Report generation failed:", err);
  toast({ variant: "destructive", title: "Report Failed", description: err.message });
}
```

};

const handleImportExcel = () =\> {
// ... keep existing
console.log("Import Excel placeholder");
toast({ title: "Import", description: "Excel import functionality to be connected." });
};

const handleExportExcel = () =\> {
// ... keep existing
console.log("Export Excel placeholder");
toast({ title: "Export", description: "Excel export functionality to be implemented." });
};

const handleExportPDF = () =\> {
// ... keep existing
console.log("Export PDF placeholder clicked. This should trigger a download, not navigation.");
toast({ title: "Export", description: "Simple PDF export functionality to be implemented." });
};

if (error && \!isLoading) {
toast({ variant: "destructive", title: "Error", description: `Could not load plants: ${error.message}` });
console.error("Error in useQuery:", error);
}

return (
\<div className="container mx-auto p-4 md:p-8 bg-[\#F5F7F5] min-h-screen"\>
\<header className="mb-8"\>
{/\* ... keep existing header ... \*/}
\<h1 className="text-4xl font-bold text-[\#2E7D32] text-center"\>GreenStock Manager\</h1\>
\<p className="text-center text-lg text-[\#333333] mt-2"\>Smart Plant Inventory Management\</p\>
\</header\>

```
  <div className="mb-6 flex flex-col sm:flex-row justify-between items-center gap-4">
    {/* ... keep existing search input ... */}
    <Input
      type="search"
      placeholder="Search by name or scientific name..."
      value={searchTerm}
      onChange={handleSearchChange}
      className="max-w-sm text-base border-gray-300 focus:ring-[#4CAF50] focus:border-[#4CAF50]"
    />
    <div className="flex gap-2 flex-wrap justify-center">
      <Button onClick={handleImportExcel} className="bg-[#4CAF50] hover:bg-[#388E3C] text-white">
        Import Excel
      </Button>
    </div>
  </div>
  
  <div className="mb-6 p-4 border border-gray-300 rounded-lg bg-white shadow">
    <h2 className="text-xl font-semibold text-[#2E7D32] mb-3">Data Exports & Reports</h2>
    <div className="flex flex-col gap-4"> {/* Changed to flex-col for better layout with checkbox */}
      <div className="flex flex-col sm:flex-row flex-wrap gap-4 items-center">
        <Button onClick={handleExportExcel} className="bg-[#2E7D32] hover:bg-[#1B5E20] text-white">
          Export Inventory (Excel)
        </Button>
        <Button onClick={(e) => handleExportPDF()} className="bg-[#81C784] hover:bg-[#66BB6A] text-black">
          Export Inventory (Simple PDF)
        </Button>
        <Button 
          onClick={(e) => handleGenerateCultivationReport(e)}
          className="bg-teal-600 hover:bg-teal-700 text-white flex items-center gap-2"
        >
          <Download size={18} /> ΔΗΛΩΣΗ ΚΑΛΛΙΕΡΓΕΙΑΣ (PDF)
        </Button>
      </div>
      <div className="flex items-center space-x-2 mt-3 sm:mt-0 sm:ml-4"> {/* Checkbox below or to the side */}
        <Checkbox
          id="excludeZero"
          checked={excludeZeroQuantity}
          onCheckedChange={(checked) => setExcludeZeroQuantity(Boolean(checked))}
          className="border-gray-400 data-[state=checked]:bg-[#2E7D32]"
        />
        <Label htmlFor="excludeZero" className="text-sm font-medium text-[#333333]">
          Exclude plants with zero quantity from ΔΗΛΩΣΗ ΚΑΛΛΙΕΡΓΕΙΑΣ
        </Label>
      </div>
    </div>
  </div>

  <div className="mb-6 p-4 border border-gray-300 rounded-lg bg-white shadow">
    {/* ... keep existing backup/restore section ... */}
    <h2 className="text-xl font-semibold text-[#2E7D32] mb-3">Backup & Restore</h2>
    <div className="flex flex-col sm:flex-row gap-4 items-center">
      <Button onClick={handleBackupData} className="bg-blue-600 hover:bg-blue-700 text-white">
        Backup Plant Data (JSON)
      </Button>
      
      <AlertDialog>
        <AlertDialogTrigger asChild>
          <Button variant="outline" className="border-orange-500 text-orange-600 hover:bg-orange-50">
            Restore Plant Data (JSON)
          </Button>
        </AlertDialogTrigger>
        <AlertDialogContent>
          <AlertDialogHeader>
            <AlertDialogTitle>Are you absolutely sure?</AlertDialogTitle>
            <AlertDialogDescription>
              This action will **delete all current plant data** and replace it with data from the backup file. 
              This cannot be undone. Please ensure you have selected the correct backup file.
            </AlertDialogDescription>
          </AlertDialogHeader>
          <AlertDialogFooter>
            <AlertDialogCancel>Cancel</AlertDialogCancel>
            <AlertDialogAction 
              onClick={() => fileInputRef.current?.click()} 
              className="bg-orange-500 hover:bg-orange-600"
              disabled={isRestoring}
            >
              {isRestoring ? "Restoring..." : "Choose File & Restore"}
            </AlertDialogAction>
          </AlertDialogFooter>
        </AlertDialogContent>
      </AlertDialog>
      <input 
        type="file" 
        accept=".json" 
        onChange={handleRestoreData} 
        className="hidden" 
        ref={fileInputRef}
        disabled={isRestoring}
      />
    </div>
    {isRestoring && <p className="mt-2 text-orange-600">Restoring data, please wait...</p>}
  </div>

  {/* ... (keep existing plant table rendering logic) ... */}
  {isLoading && <p className="text-center text-[#333333]">Loading plant inventory...</p>}
  {error && <p className="text-center text-red-600">Error loading data. Please try again.</p>}
  
  {!isLoading && !error && plants && (
    <div className="overflow-x-auto bg-white shadow-md rounded-lg">
      <Table>
        <TableHeader className="bg-[#E8F5E9]">
          <TableRow>
            <TableHead className="text-[#2E7D32] font-semibold">Name</TableHead>
            <TableHead className="text-[#2E7D32] font-semibold">Scientific Name</TableHead>
            <TableHead className="text-[#2E7D32] font-semibold text-right">Planting Year</TableHead>
            <TableHead className="text-[#2E7D32] font-semibold text-right">Quantity</TableHead>
            <TableHead className="text-[#2E7D32] font-semibold">Actions</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          {plants.length > 0 ? plants.map((plant) => (
            <TableRow key={plant.id} className="hover:bg-[#F5F7F5]">
              <TableCell className="text-[#333333]">{plant.name}</TableCell>
              <TableCell className="text-[#333333]">{plant.scientificName || 'N/A'}</TableCell>
              <TableCell className="text-[#333333] text-right">{plant.plantingYear || 'N/A'}</TableCell>
              <TableCell className="text-[#333333] text-right">{plant.quantity}</TableCell>
              <TableCell>
                <Button variant="outline" size="sm" className="mr-2 border-[#81C784] text-[#2E7D32] hover:bg-[#E8F5E9]">Edit</Button>
                <Button variant="destructive" size="sm" className="bg-red-500 hover:bg-red-700">Delete</Button>
              </TableCell>
            </TableRow>
          )) : (
            <TableRow>
              <TableCell colSpan={5} className="text-center text-gray-500 py-8">
                No plants found matching your search criteria or inventory is empty.
              </TableCell>
            </TableRow>
          )}
        </TableBody>
      </Table>
    </div>
  )}
   {!isLoading && !error && !plants && (
     <p className="text-center text-gray-500 py-8">No plants available in the inventory.</p>
   )}
</div>
```

);
};

export default Index;
\</lov-write\>

**Step 2: Update Backend API (`src/pages/api/reports/cultivation-declaration.ts`)**
We'll modify the database query based on the new parameter.

\<lov-write file\_path="src/pages/api/reports/cultivation-declaration.ts"\>
import type { NextApiRequest, NextApiResponse } from 'next';
import { db } from '@/server/db'; // Adjust path to your db setup
import { plantsTable } from '@/server/db/schema'; // Adjust path to your schema
import { asc, gt } from 'drizzle-orm'; // Import gt for "greater than"
import { PDFDocument, rgb, PageSizes, PDFFont } from 'pdf-lib';
import fs from 'fs';
import path from 'path';

const drawTextSafe = (
page: any,
text: any,
x: number,
y: number,
font: PDFFont,
size: number,
maxWidth: number,
color = rgb(0, 0, 0)
) =\> {
// ... (keep existing drawTextSafe function from previous step with truncation)
let safeText = text === undefined || text === null ? '' : String(text);
let textWidth = font.widthOfTextAtSize(safeText, size);
if (textWidth \> maxWidth && maxWidth \> 0) {
let truncatedText = '';
for (let i = safeText.length; i \> 0; i--) {
truncatedText = safeText.substring(0, i) + '...';
if (font.widthOfTextAtSize(truncatedText, size) \<= maxWidth) {
break;
}
}
safeText = truncatedText;
if (font.widthOfTextAtSize(safeText, size) \> maxWidth) {
safeText = safeText.substring(0, Math.floor(maxWidth / font.widthOfTextAtSize("...", size) \* 3));
}
}
page.drawText(safeText, { x, y, font, size, color });
};

export default async function handler(
req: NextApiRequest,
res: NextApiResponse
) {
if (req.method === 'GET') {
const { excludeZero } = req.query; // Get the new query parameter
console.log(`API: Cultivation Report. Exclude zero quantity: ${excludeZero}`);

```
try {
  const fontPath = path.join(process.cwd(), 'public', 'fonts', 'NotoSansGreek-Regular.ttf');
  let customFontBytes;
  try {
    customFontBytes = fs.readFileSync(fontPath);
  } catch (fontError: any) {
    console.error(`API: Failed to load font file at ${fontPath}. Error: ${fontError.message}`);
    return res.status(500).json({ 
      message: 'Failed to load font for PDF generation. Ensure font file is in public/fonts/.', 
      error: `Font file not found: ${fontPath}.` 
    });
  }

  // Base query
  let query = db.select().from(plantsTable);

  // Conditionally add where clause
  if (excludeZero === 'true') {
    console.log("API: Excluding plants with zero quantity.");
    // @ts-ignore // Drizzle's .where() can be chained, this ensures it works type-wise
    query = query.where(gt(plantsTable.quantity, 0));
  }
  
  // @ts-ignore // orderBy can be chained after where
  const plantsData = await query.orderBy(asc(plantsTable.name));
  console.log(`API: Fetched ${plantsData.length} plants for the report.`);

  const pdfDoc = await PDFDocument.create();
  const customFont = await pdfDoc.embedFont(customFontBytes);
  
  let page = pdfDoc.addPage(PageSizes.A4);
  const { width, height } = page.getSize();
  
  const fontSize = 9;
  const headerFontSize = 10;
  const titleFontSize = 14;
  const margin = 40;
  let yPosition = height - margin - 20;

  const reportTitle = "ΔΗΛΩΣΗ ΚΑΛΛΙΕΡΓΕΙΑΣ ΓΙΑ ΤΟ 2025";
  const titleWidth = customFont.widthOfTextAtSize(reportTitle, titleFontSize);
  page.drawText(reportTitle, {
    x: (width - titleWidth) / 2,
    y: yPosition,
    font: customFont,
    size: titleFontSize,
    color: rgb(0, 0, 0),
  });
  yPosition -= (titleFontSize + 25);

  const headers = [
    { text: 'Α/Α', width: 35, xOffset: 5 },
    { text: 'Name', width: 170, xOffset: 5 },
    { text: 'Scientific Name', width: 170, xOffset: 5 },
    { text: 'Year', width: 60, xOffset: 5 },
    { text: 'Quantity', width: 60, xOffset: 5 },
  ];

  const rowHeight = 18;

  const drawPageHeaders = (targetPage: any, currentY: number) => {
    // ... (keep existing drawPageHeaders function)
    let x = margin;
    targetPage.drawLine({ start: { x: margin, y: currentY + 5 }, end: { x: width - margin, y: currentY + 5 }, thickness: 1, color: rgb(0,0,0) });
    headers.forEach(header => {
        drawTextSafe(targetPage, header.text, x + header.xOffset, currentY - headerFontSize + 2, customFont, headerFontSize, header.width - header.xOffset - 2, rgb(0,0,0));
        x += header.width;
    });
    targetPage.drawLine({ start: { x: margin, y: currentY - rowHeight + 7 }, end: { x: width - margin, y: currentY - rowHeight + 7 }, thickness: 1, color: rgb(0,0,0) });
    return currentY - rowHeight;
  };
  
  yPosition = drawPageHeaders(page, yPosition + rowHeight); // Draw headers on the first page

  plantsData.forEach((plant, index) => {
    // ... (keep existing row drawing logic, including new page creation and header redraw)
    if (yPosition < margin + rowHeight) {
      page.drawLine({start: {x: margin, y: yPosition + 7}, end: {x: width-margin, y: yPosition + 7}, thickness: 0.5, color: rgb(0.7,0.7,0.7) });
      page = pdfDoc.addPage(PageSizes.A4);
      yPosition = height - margin - rowHeight - 10; 
      yPosition = drawPageHeaders(page, yPosition + rowHeight);
    }

    let currentX = margin;
    const rowData = [
      { text: (index + 1).toString(), width: headers[0].width, xOffset: headers[0].xOffset },
      { text: plant.name, width: headers[1].width, xOffset: headers[1].xOffset },
      { text: plant.scientificName || '', width: headers[2].width, xOffset: headers[2].xOffset },
      { text: plant.plantingYear?.toString() || '', width: headers[3].width, xOffset: headers[3].xOffset },
      { text: plant.quantity.toString(), width: headers[4].width, xOffset: headers[4].xOffset },
    ];

    rowData.forEach(data => {
      const cellContentMaxWidth = data.width - data.xOffset - 2; 
      drawTextSafe(page, data.text, currentX + data.xOffset, yPosition - fontSize + 2 , customFont, fontSize, cellContentMaxWidth);
      currentX += data.width;
    });
    page.drawLine({start: {x: margin, y: yPosition - rowHeight + 7}, end: {x: width-margin, y: yPosition -rowHeight + 7}, thickness: 0.5, color: rgb(0.7,0.7,0.7)});
    yPosition -= rowHeight;
  });

  const pdfBytes = await pdfDoc.save();
  const timestamp = new Date().toISOString().replace(/:/g, '-').slice(0, 19);
  const filename = `declaration-cultivation-2025-${excludeZero === 'true' ? 'nozero-' : ''}${timestamp}.pdf`;

  res.setHeader('Content-Disposition', `attachment; filename="${filename}"`);
  res.setHeader('Content-Type', 'application/pdf');
  res.status(200).send(Buffer.from(pdfBytes));
  console.log(`API: Cultivation Declaration Report sent. Excluded zero: ${excludeZero}`);

} catch (error: any) {
  console.error('API: Error generating Cultivation Declaration PDF:', error);
  res.status(500).json({ message: 'Failed to generate PDF report', error: error.message });
}
```

} else {
res.setHeader('Allow', ['GET']);
res.status(405).end(`Method ${req.method} Not Allowed`);
}
}
\</lov-write\>
I've added the checkbox to the "Data Exports & Reports" section on your main page. When you check it and generate the "ΔΗΛΩΣΗ ΚΑΛΛΙΕΡΓΕΙΑΣ (PDF)" report, it will now only include plants with a quantity greater than zero. The backend API has been updated to filter the data accordingly based on this new option.
\</lov-code\>

Great\! I've added the option to exclude plants with zero quantity from your "ΔΗΛΩΣΗ ΚΑΛΛΙΕΡΓΕΙΑΣ ΓΙΑ ΤΟ 2025" PDF report.

On the main page, under the "Data Exports & Reports" section, you'll now find a checkbox:
"Exclude plants with zero quantity from ΔΗΛΩΣΗ ΚΑΛΛΙΕΡΓΕΙΑΣ".

If you check this box before clicking the "ΔΗΛΩΣΗ ΚΑΛΛΙΕΡΓΕΙΑΣ (PDF)" button, the generated report will only list plants that have a quantity greater than zero. If you leave it unchecked, all plants (including those with zero quantity) will be included, sorted alphabetically as before.

This should give you more flexibility in tailoring the report to your specific needs\!